import { Route } from '@angular/router';
import { SignalTableComponent } from './signal-table.component';

export const appRoutes: Route[] = [
    {
      path: '',
      component: SignalTableComponent
    }
  ];